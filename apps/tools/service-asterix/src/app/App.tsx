import React from 'react';

const style = {
  background: '#800',
  color: '#fff',
  padding: 12,
};

const Button = () => <button style={style}>This is service Asterix coming from React SPA app</button>;

export default Button;